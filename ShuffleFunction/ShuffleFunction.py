#Importamos la librería random
import random

# Creamos la lista
letters = ['a', 'b', 'c']

#Usamos esta utiliza para aleatorizar el orden de los elementos en una lista
random.shuffle(letters)

#Imprimimos la lista
print(letters)